//This file gets run automatically by Jest before every test and therefore you don’t
// need to add the imports to every single test file.
import 'jest-dom/extend-expect'

// this is basically: afterEach(cleanup)
import 'react-testing-library/cleanup-after-each'
